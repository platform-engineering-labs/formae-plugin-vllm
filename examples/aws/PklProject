amends "pkl:Project"

dependencies {
  ["vllm"] = import("../../schema/pkl/PklProject")
  ["formae"] {
    uri = "package://hub.platform.engineering/plugins/pkl/schema/pkl/formae/formae@0.86.0"
  }
  ["aws"] {
    uri = "package://hub.platform.engineering/plugins/aws/schema/pkl/aws/aws@0.1.11"
  }
  ["compose"] {
    // Dev releases publish their PKL schema WITHOUT the -dev suffix:
    // the 0.1.3-dev.0 tag publishes its schema as compose@0.1.3.
    uri = "package://hub.platform.engineering/plugins/compose/schema/pkl/compose/compose@0.1.3"
  }
}

amends "pkl:Project"

package {
  name = "vllm"
  baseUri = "package://hub.platform.engineering/plugins/vllm/schema/pkl/\(name)/\(name)"
  version = read("VERSION").text.trim()
  packageZipUrl = "https://hub.platform.engineering/plugins/vllm/schema/pkl/\(name)/\(name)@\(version).zip"
}

dependencies {
  ["formae"] {
    uri = "package://hub.platform.engineering/plugins/pkl/schema/pkl/formae/formae@0.84.0"
  }
}

amends "pkl:Project"

// Package configuration for local development.
// Update baseUri and packageZipUrl when publishing to a registry.
package {
  name = "vllm"
  baseUri = "package://localhost/plugins/vllm/schema/pkl/vllm"
  version = "0.1.0"
  packageZipUrl = "https://localhost/plugins/vllm/schema/pkl/vllm@\(version).zip"
}

dependencies {
  ["formae"] {
    uri = "package://hub.platform.engineering/plugins/pkl/schema/pkl/formae/formae@0.84.0"
  }
}
